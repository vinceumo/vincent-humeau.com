---
type: projects
title: 💻 Projects
order: 10
---

import data from "./projects.js";


<div>{data.projects.map(project => {
    return <div key={project.title}>
    <p>{project.year}</p>
    <h3>{project.title}</h3>
    <p>{project.role}</p>
    {project.description && <p>{project.description}</p>}
    <ul>
        {project.technology.map(t => <li key={t}>{t}</li>)}
    </ul>
    {project.links && <ul>
        {project.links.map(l => <li key={l.name}><a href={l.url}>{l.name}</a></li>)}
    </ul>}
    </div>
})}</div>